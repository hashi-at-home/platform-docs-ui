[tools]
node = "24.20.0"
"npm:gulp" = "5"
"npm:semantic-release" = "25.0.9"

prek = "0.4.12"

[tasks.prepare-release]
description = "Add release dependencies"
run = "npm install \"@semantic-release/exec@7.1.0\""
[tasks.release]
description = "Create a Semantic Release"
run = "semantic-release"
